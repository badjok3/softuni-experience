function getLowestPrice(markets) {
    let result = new Map();

    for(let market of markets) {
        [town, product, price] = market;
        result.set(product, new Map());
       // result.product
    }
}